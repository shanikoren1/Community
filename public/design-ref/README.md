# Drop your design assets here 📥

Save these files into this folder (`public/design-ref/`) and tell me — I'll read them
and build the site to match.

## From Stitch (most important — this is the visual truth)
1. **Logo** — download the Become logo as PNG or SVG → save as `logo.png` (or `logo.svg`).
2. **Screenshots of each screen**, especially the **mobile / phone view**:
   - `home-mobile.png`, `home-desktop.png`
   - `survey-mobile.png` (and any other screens)
   - In Stitch: open the screen → screenshot it (or use Stitch's export/download).
3. **(Bonus) Exported code** — if Stitch has a "</> Code" or "Export" button, copy it
   into a file here called `stitch-export.txt`. This makes the match exact.

## From your Google Form (the survey)
- Easiest: a screenshot of each question → `survey-q1.png`, `survey-q2.png`, …
- Or paste the questions to me as text.

## Tip
You can drag files straight into this folder in VS Code. Once they're here, just say
"the files are in design-ref" and I'll take it from there.
